<h3 class="text-center">投稿された本の一覧</h3>
<div class="row">
    <div class="col-sm-6">
        <p class="p-1 m-0 mt-1"><%= link_to @book.user.name, user_path(@book.user) %>　さんの投稿</p>
        <div class="card m-1">
            <h5 class="card-header p-1"><%= @book.name %>  <small class="text-muted">著者： <%= @book.author %></small></h5>
            <div class="card-boby m-1 p-1">
                <p class="card-text"><span class="badge badge-secondary">説明</span><br><%= @book.description %></p>
                <!--ボタン-->
                <span class="badge badge-primary">Good<i class="fas fa-thumbs-up"></i>：<span class="badge badge-dark"><%= @count_good_comments %></span></span>
                <span class="badge badge-warning">Bad<i class="fas fa-thumbs-down"></i>：<span class="badge badge-da"><%= @count_bad_comments %></span></span>
            </div>
            <div class="card-footer p-1 m-0">
                 <small class="text-muted m-0">投稿日:<%= @book.created_at.strftime('%Y/%m/%d') %></small>
            </div>
        </div>
    <div ="text_center">
        <%= link_to 'コメントする', comment_new_book_path(@book), class: 'btn btn-primary btn-block' %>　<!--オリジナル課題専用-->
    </div>
        
        <h3 class="text_center">コメント一覧：　<%= @count_comments %> 件</h3>
        <%= render 'comments/comments', comments: @comments %>
    </div> 
</div>